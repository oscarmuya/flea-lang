let x = 5;
let y = 10;
let z = "hello";

while (x < y) {
    print x;
    x = x + 1;
}

func process();

func process(amount,count){

}

process(200,300);

if (x <= 10){
    print "Yay its more!";
} else {
    print "Nay its less!";
}

// this is a comment
